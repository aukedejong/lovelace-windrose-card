export class CurrentDirectionConfig {

    constructor(
        public readonly showArrow: boolean,
        public readonly arrowSize: number | undefined,
        public readonly centerCircleSize: number | undefined) {
    }

}
